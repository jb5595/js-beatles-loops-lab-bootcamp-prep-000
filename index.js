function theBeatlesPlay(musicians, instruments){
  var sentances = new Array();
  for (let i = 0; i <musicians.length -1; i++){
   sentance[i] = "${i}"
   // sentances[i] = '${musicians[i]} plays $[instruments[i]]'
  }
  return sentances;
}